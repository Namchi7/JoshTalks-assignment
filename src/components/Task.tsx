import React from "react";
import styles from "@/styles/task.module.css";

const Task: React.FC = () => {
  return (
    <div className={styles.container}>
      <div>Task</div>
    </div>
  );
};

export default Task;
